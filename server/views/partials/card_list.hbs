{{#if cards}}
    <table class="table table-striped table-hover table-sm">
        <thead>
        <tr>

            <td>Staged</td>
            <td>Word</td>
            <td>Translation</td>
            <td>Actions</td>
        </tr>
        </thead>
        <tbody>
        {{#each cards}}
            <tr>
                <td>
            {{#if isStaged}}
                <span class="oi oi-circle-check" aria-hidden="true"></span>
            {{else}}
            {{/if}}
                </td>
                <td>{{wordDe}}</td>
                <td>{{wordEn}}</td>
                <td>[<a href="{{url}}">edit</a>] [<a href="{{url}}/delete">delete</a>]</td>
            </tr>
        {{/each}}
        </tbody>
    </table>
{{/if}}
